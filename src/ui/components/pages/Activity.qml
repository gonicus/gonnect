pragma ComponentBehavior: Bound

import QtQuick
import QtQuick.Controls.Material
import base

Item {
    id: control

    Label {
        anchors.centerIn: parent
        text: 'TODO: Activity page'
    }
}
